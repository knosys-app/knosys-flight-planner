node_modules
dist
main.js.map
data/cache
.DS_Store
*.log
.vite
coverage

# main.js and styles.css ARE checked in \u2014 the Knosys installer's main-branch
# fallback reads them from the repo root. Releases remain the primary path.

# data/airports.sqlite IS checked in \u2014 fetched at plugin first-run via
# raw.githubusercontent.com (CORS-friendly; release-assets CDN is not).
